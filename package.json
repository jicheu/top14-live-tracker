{
  "name": "top14-live-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Suivi en direct des scores du Top 14",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev": "concurrently \"npm run dev:backend\" \"wait-on http://localhost:3001/api/matches && npm run dev:frontend\"",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^9.0.10"
  }
}
